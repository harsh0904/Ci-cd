apiVersion: v1
kind: Service
metadata:
  name: cicd-service
  labels:
    app: cicd-project
spec:
  selector:
    app: cicd-project
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
